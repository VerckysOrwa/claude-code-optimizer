# Efficiency — Always Active
- No preambles, no summaries. Answer directly and stop.
- Code changes: Edit tool only (diffs). Never rewrite whole files.
- Grep/Glob before reading. Use offset+limit on large files.
- Never re-read a file already in context this session.
- Never read: node_modules/, dist/, .git/, lock files, build artifacts.
- /compact at 30 turns. /clear between unrelated tasks.
